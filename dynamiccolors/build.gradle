plugins {
    id 'com.android.library'
}

android {
    namespace = "com.android.systemui.dynamiccolors"

    sourceSets {
        main {
            res.srcDirs = ['res']
            manifest.srcFile 'AndroidManifest.xml'
        }
    }
}
